<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="300dp"
            android:src="@drawable/fatal_error" />

        <TextView
            android:id="@+id/tvFatalError"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="24dp"
            android:gravity="center"
            android:textSize="21sp"
            tools:text="@string/network_internet_not_connected" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnErrorRetry"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/retry"
            android:textColor="?attr/baseThemeComplimentColor"
            app:backgroundTint="?attr/baseThemeColor"
            app:cornerRadius="10dp"
            app:strokeColor="@color/yellow500"
            app:strokeWidth="3dp" />

    </LinearLayout>

</ScrollView>